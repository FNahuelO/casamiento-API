import { STRING, UUID, ARRAY, BOOLEAN, UUIDV4 } from "sequelize";
import sequelize from "../config/sequelize.js";

const InvitadoBebida = sequelize.define("InvitadoBebida", {});

export default InvitadoBebida;
